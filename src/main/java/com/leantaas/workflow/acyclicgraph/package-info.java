/**
 * Acyclic graph classes are used to define correct behavior of acyclic graph.
 *
 * <p>Graph node have two kind of edges, edges leads to children, and edges leads to parents. They can optionally
 * function as undirected graph. But in workflow task, we should mainly focus on its single direction characteristics.
 */
package com.leantaas.workflow.acyclicgraph;